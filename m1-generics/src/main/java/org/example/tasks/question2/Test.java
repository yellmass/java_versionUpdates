package org.example.tasks.question2;

import javax.sql.rowset.Predicate;
import java.util.Arrays;
import java.util.List;

public class Test {
    /*public static void main(String[] args) {
        List<Integer> ci = Arrays.asList(1, 2, 3, 4);
        int count = countIf(ci, p-> p%2 != 0 );
        System.out.println("Number of odd integers = " + count);
    }

    private static <T> int countIf(List<T> ci, Predicate<T> oddPredicate) {
        int count = 0;
        for (T t : ci) {
            if (oddPredicate.evaluate()){
                count++;
            }
        }
        return count;
    }*/


}

/*
List<Integer> ci = Arrays.asList(1, 2, 3, 4);
int count = countIf(ci, new OddPredicate());
System.out.println("Number of odd integers = " + count);
 */
